export enum NAV_LINKS {
  HOME = "/",
  CATALOGUE = "/catalogue",
  ABOUT = "/about",
  CONTACTS = "/contacts",
  LOGIN = "/login",
}

export enum CATALOGUE_FILTERS {
  SEARCH = "search",
}

export enum FORM_FIELDS {
  PRODUCT_NAME = "product-name",
}
